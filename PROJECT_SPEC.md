# 声学测线调查服务

## 项目目标
声学测线调查服务帮助近岸调查团队在一次声学采样作业中建立测线、记录回波读数、跟踪质量告警并形成可复核的作业摘要。服务只使用进程内数据，适合演示现场采样和质量核验流程。

## 用户角色
- **调查技术员**：创建测线、启动采样、提交声学读数和结束作业。
- **声学分析员**：查看有效读数、质量评分、频段分布和作业摘要。
- **质量复核员**：查看校准区间、低信噪比或读数跳变产生的告警。

## 核心实体
- **调查测线（Survey）**：标识、海区、目标频段、状态、时间边界和读数数量。
- **声学读数（Reading）**：采样时刻、频率、回波强度、噪声、深度和归一化强度。
- **质量告警（Alert）**：读数违反质量规则时形成的严重程度和说明。
- **作业摘要（Summary）**：有效读数、平均归一化强度、深度覆盖、频段分布和未解决告警。

## 业务流程
### 1. 建立调查测线
调查技术员提交测线标识、海区和目标频段。服务校验数据并保存草稿测线；草稿随后可转为活动状态开始采样。

### 2. 记录声学读数
技术员向活动测线提交频率、回波强度、噪声和深度。服务归一化读数、计算质量评分，并按校准范围、信噪比和相邻变化生成告警。

### 3. 结束作业并查看摘要
技术员结束已包含读数的活动测线。服务锁定后续写入，重新计算有效读数、平均强度、覆盖深度、频段计数和告警数量，供分析员复核。

## 状态与规则
- 测线状态按 `draft`、`active`、`closed` 流转；仅草稿可以启动，只有包含读数的活动测线可以结束。
- 读数频率必须为正，深度必须为正，噪声不得高于回波强度。
- 低信噪比、超出校准窗口和相邻归一化读数大幅变化都会形成告警，但读数会保留供复核。
- 已结束的测线拒绝新读数；摘要从当前保存读数重新计算。

## 接口与验证
- `GET /health` 返回服务状态，`GET /v1/bands` 返回声学频段资料。
- `POST /v1/surveys` 与 `POST /v1/surveys/{id}/activate` 验证建立和启动流程。
- `POST /v1/surveys/{id}/readings` 验证读数标准化和质量告警流程。
- `POST /v1/surveys/{id}/close` 与 `GET /v1/surveys/{id}/summary` 验证结束作业和摘要流程。
- `go test ./...` 覆盖状态转换、读数边界、质量规则、摘要和 HTTP 输入。
