# 项目与标准命令

项目是声学测线调查服务，标准编译命令为 `go build ./...`，标准测试命令为 `go test ./...`。

# 环境构建与编译

在当前平台使用 `golang:1.26.2` 构建镜像，执行 `go build ./...` 成功。

# 故障触发步骤

创建一条测线并启动采样，然后提交一条 `captured_at` 早于启动时间的回波读数，最后读取该测线摘要。

# 实际错误输出

定向测试失败：`reading captured before activation was accepted`

# 期望行为

测线启动前采集的缓存读数应被拒绝，摘要不应增加该样本；启动后的正常读数仍应按原流程保存。
